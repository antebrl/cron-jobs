name: Daily API Request

on:
  schedule:
    - cron: "0 0 * * *"  # Täglich um 12:00 UTC (13:00 MEZ / 14:00 MESZ)

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      - name: API Request ausführen
        run: |
          curl -X DELETE "https://ante.is-a.dev/api/channels/clear"
